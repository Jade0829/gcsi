kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: gluster-vol-1
spec:
  storageClassName: kadalu.gluster-vol
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
